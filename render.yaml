services:
  - type: web
    name: image-upscaler
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
    healthCheckPath: /health
    autoDeploy: true